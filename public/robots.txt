# This is an internal prototype, not a launch. Nothing here should be crawled,
# indexed, or previewed as a link card anywhere.
#
# It used to Allow: / for Googlebot, Bingbot, Twitterbot and facebookexternalhit
# by name, which is how an internal URL ends up in search results and in link
# previews. Reverse this file — and the noindex in index.html — deliberately, on
# the day the real site launches. Until then, closed.

User-agent: *
Disallow: /
